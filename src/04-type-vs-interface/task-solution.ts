type UserType = {
  id: number;
};

type UserType = {
  name: string;
};

interface UserInterface {
  id: number;
}

interface UserInterface {
  name: string;
}

const user: UserInterface = {
  id: 1,
  name: "Peter",
};
