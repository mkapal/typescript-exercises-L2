// The type parameter `TMessage` is not needed because it only appeared once
// Type parameters are used to relate types of multiple values

function logMessage(message: string) {
  console.log(message);
}
